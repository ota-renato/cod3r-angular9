import { RedDirective } from './red.directive';

describe('RedDirective', () => {
  it('should create an instance', () => {
    const directive = new RedDirective(null);
    expect(directive).toBeTruthy();
  });
});
